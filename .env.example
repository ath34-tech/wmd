# Copy to .env and fill in. All values are optional except GEMINI_API_KEY,
# which POST /api/meals/analyze needs (it returns 503 without it).
GEMINI_API_KEY=
# GEMINI_MODEL=gemini-3.1-flash-lite
# DATABASE_URL=sqlite:///./wmd.db
# UPLOAD_DIR=./uploads
# MAX_UPLOAD_BYTES=5242880
